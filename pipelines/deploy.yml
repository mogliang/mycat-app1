# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

pool:
  vmImage: ubuntu-latest

steps:
- task: ShellScript@2
  name: loadMetadata
  displayName: 'Load metadata'
  inputs: 
    scriptPath: $(Build.SourcesDirectory)/pipelines/scripts/load-env-variable.sh

- task: Kubernetes@1
  displayName: Deploy app1
  inputs:
    connectionType: 'Kubernetes Service Connection'
    kubernetesServiceEndpoint: '$(loadMetadata.kubeConnection)'
    command: 'apply'
    arguments: '-k $(Build.SourcesDirectory)/kustomize/environments/$(deployEnvironment)'
    secretType: 'dockerRegistry'
    containerRegistryType: 'Azure Container Registry'